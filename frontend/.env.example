# ES: Copia como .env y ajusta. EN: Copy as .env and adjust.

# Base URL of the backend API, including the /api prefix.
# Local development (uvicorn on the default port):
VITE_API=http://127.0.0.1:8000/api

# Deployed demo — point this at the backend host, e.g.:
# VITE_API=https://outdaxius-api.onrender.com/api
